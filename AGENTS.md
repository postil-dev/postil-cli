# Postil rollout rules

Use additive, backward-compatible changes. Keep hosted reviews available during
rolling deployment and rollback. Add schema, API, configuration, and release
capabilities alongside existing behavior; remove obsolete behavior only after
consumer migration and separate compatibility verification.
