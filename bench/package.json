{
  "name": "postil-bench",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "description": "Hermetic PR-review regression suite for the postil CLI (mock forge + mock model).",
  "scripts": {
    "bench": "bun run src/run.ts",
    "bench:live": "bun run src/run.ts --live",
    "bench:live-models": "POSTIL_BENCH_MODE=live bun run src/run.ts",
    "verify-admission": "bun run src/verify-admission.ts",
    "scorer-eval": "bun run src/scorer-eval.ts",
    "cost-guard": "bun run src/cost-guard.ts"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
